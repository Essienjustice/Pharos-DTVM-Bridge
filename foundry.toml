[profile.default]
src = "contracts/evm"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc_version = "0.8.25"
optimizer = true
optimizer_runs = 200
evm_version = "paris"
bytecode_hash = "none"

[rpc_endpoints]
testnet = "${PHAROS_TESTNET_RPC}"
mainnet = "${PHAROS_MAINNET_RPC}"
